# Caddy obtains and renews the HTTPS certificate for this domain by itself,
# as long as the domain's DNS points at this server and ports 80/443 are open.
{$TRIAGEQ_DOMAIN} {
	encode gzip
	request_body {
		max_size 60MB
	}
	reverse_proxy app:8501
}
