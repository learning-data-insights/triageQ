# triageQ (v1.1.0)
#
#   pip install -r requirements.txt

# ── Core ──────────────────────────────────────────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0

# ── Model provider (install what you plan to use — see model_providers.py) ────
# anthropic  needed for the Anthropic (Claude) provider
# openai     needed for the OpenAI provider AND the OpenAI-compatible provider
#            (local models, self-hosted servers, any /chat/completions endpoint)
# Only used lazily by the provider you actually select in Settings, so it's
# fine to install just one — but leaving both in is harmless and simplest if
# you might switch later.
anthropic>=0.40.0
openai>=1.0.0

# ── Local file library ────────────────────────────────────────────────────────
# pypdf        reads the first pages of a local PDF to pull its title, authors,
#              and DOI for matching, and (for text-only providers) extracts the
#              full text sent for screening in place of native PDF vision.
# python-pptx  reads PowerPoint decks (.pptx / .pptm).
pypdf>=4.0.0
python-pptx>=0.6.23

# ── Optional ──────────────────────────────────────────────────────────────────
# Drag-and-drop onto the Batch tab. Without it the drop zone becomes a label
# and the Browse… button does the same job.
tkinterdnd2>=0.3.0

# NOT a pip package: LibreOffice.
#   Needed to convert PowerPoint to PDF (keeps figures and charts, which text
#   extraction drops) and to open legacy binary .ppt at all. Without it, .pptx
#   decks fall back to text extraction and .ppt files are reported unreadable.
#
#   macOS    brew install --cask libreoffice
#   Windows  https://www.libreoffice.org/download/
#   Linux    sudo apt install libreoffice
#
# Settings → Local File Library shows which of these are actually present.
