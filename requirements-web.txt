# triageQ — hosted web build (web_app.py). The desktop app uses requirements.txt.
#
#   pip install -r requirements-web.txt
#   streamlit run web_app.py

streamlit>=1.40.0
requests>=2.31.0
beautifulsoup4>=4.12.0
anthropic>=0.40.0
openai>=1.0.0
pypdf>=4.0.0
python-pptx>=0.6.23

# Not included: tkinterdnd2 (desktop drag-and-drop) and LibreOffice. Without
# LibreOffice, .pptx decks are screened from their slide text; .ppt is not
# accepted on the web.
